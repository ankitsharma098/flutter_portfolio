import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static const lightPrimary = Color(0xFF2563EB);    // Primary blue
  static const lightSecondary = Color(0xFF3B82F6);  // Lighter blue for secondary elements
  static const lightBackground = Color(0xFFFFFFFF);  // Pure white
  static const lightSurface = Color(0xFFF3F4F6);    // Light gray for cards/inputs
  static const lightError = Color(0xFFDC2626);      // Error red
  static const lightSuccess = Color(0xFF059669);    // Success green
  static const lightWarning = Color(0xFFF59E0B);    // Warning yellow
  static const lightText = Color(0xFF1F2937);       // Dark gray for text
  static const lightSecondaryText = Color(0xFF6B7280); // Medium gray for secondary text
  static const lightDisabled = Color(0xFFE5E7EB);   // Disabled state color
  static const lightDivider = Color(0xFFE5E7EB);
  static const lightDeepPurple = Color(0xFFB05DDE);

  // Dark Theme Colors
  static const darkPrimary = Color(0xFF93C5FD);     // Light blue
  static const darkSecondary = Color(0xFF60A5FA);   // Secondary blue
  static const darkBackground = Color(0xFF111827);   // Very dark blue-gray
  static const darkSurface = Color(0xFF1F2937);     // Slightly lighter dark blue-gray
  static const darkError = Color(0xFFFCA5A5);       // Softer red for dark theme
  static const darkSuccess = Color(0xFF34D399);     // Softer green for dark theme
  static const darkWarning = Color(0xFFFCD34D);     // Softer yellow for dark theme
  static const darkText = Color(0xFFF9FAFB);        // Off-white for text
  static const darkSecondaryText = Color(0xFFD1D5DB); // Light gray for secondary text
  static const darkDisabled = Color(0xFF374151);     // Disabled state color
  static const darkDivider = Color(0xFF374151); //Light gray for secondary text
  static const darkDeepPurple = Color(0xFFB05DDE); //Light gray for secondary text
}